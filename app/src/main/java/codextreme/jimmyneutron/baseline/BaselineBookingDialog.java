package codextreme.jimmyneutron.baseline;

import android.app.DatePickerDialog;
import android.app.Dialog;
import android.app.Fragment;
import android.content.Context;
import android.graphics.drawable.ColorDrawable;
import android.os.Bundle;
import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;
import android.view.Window;
import android.widget.ImageView;
import android.support.v7.app.AlertDialog;

import java.util.Calendar;

//import codextreme.jimmyneutron.R;

/**
 * Created by manzelseet on 21/06/17.
 */

public class BaselineBookingDialog {
/*
    public void showDatePicker(Context thix) {
        Calendar now = Calendar.getInstance();
        DatePickerDialog dpd = DatePickerDialog.newInstance(
                thix,
                now.get(Calendar.YEAR),
                now.get(Calendar.MONTH),
                now.get(Calendar.DAY_OF_MONTH)
        );
        dpd.setVersion(DatePickerDialog.Version.VERSION_2);
        dpd.show(getFragmentManager(), "Datepickerdialog");
    }
    private AlertDialog showDialog(Context ctx) {

        AlertDialog.Builder builder =
                new AlertDialog.Builder(ctx, R.style.AppCompatAlertDialogStyle);
        builder.setTitle("SCRUM");
        builder.setView(R.layout.dialog_baseline_booking);
        builder.setPositiveButton("Confirm Booking", null);
        builder.setNegativeButton("Cancel Booking", null);
        return builder.show();
    }
*/
}